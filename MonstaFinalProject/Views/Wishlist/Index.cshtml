@model List<WishlistVM>

@{
    ViewData["Title"] = "Index";
}

<div class="headofwish">
    <div class="container">
        <h1>Wishlist</h1>
        <div class="contentabout">
            <a asp-controller="home">Home</a>
            <span style="color: white; margin: 0 10px;">></span>
            <a asp-controller="wishlist"><span>Wishlist</span></a>
        </div>
        <hr style="margin: 20px 47px 0; color: #a3a3a3;">
    </div>
</div>
<main class="cart-page cart-page-main-block inner-page-sec-padding-bottom cart-main-checkoutpage">
    <div class="wishlistDetails">
        @await Html.PartialAsync("_WishlistIndexPartial", Model)
    </div>
</main>
